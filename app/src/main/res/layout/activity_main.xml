<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <sdk.adenda.widget.AdendaButton xmlns:adenda="http://schemas.android.com/apk/res-auto"
        android:id="@+id/lock_in_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        adenda:confirm_opt_in="false"
        adenda:confirm_opt_out="false"
        adenda:opt_in_text="Enable Lock screen"
        adenda:opt_out_text="Disable Lock screen" />

    <Button
        android:id="@+id/ContactButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/lock_in_button"
        android:layout_toEndOf="@+id/lock_in_button"
        android:layout_toRightOf="@+id/lock_in_button"
        android:text="New Button" />
</RelativeLayout>
